{
  "name": "steam-prices",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Cron diario: precios del mercado de Steam → steam_market_prices. Corre en un runner porque Steam le contesta 429 a la IP de las edge functions.",
  "scripts": {
    "start": "tsx index.ts",
    "test": "node --test --experimental-strip-types lib.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3"
  }
}
