import { PagePlaceholder } from "@/ui/PagePlaceholder";

export default function CalendarPage() {
  return (
    <PagePlaceholder
      title="Calendar"
      sub="Your shows, day by day."
      note="The chronological feed of your shows arrives in Phase 2."
    />
  );
}
