import { PagePlaceholder } from "@/ui/PagePlaceholder";

export default function TonightPage() {
  return (
    <PagePlaceholder
      title="Tonight"
      sub="Fresh episodes, what to continue, and what's coming."
      note="The bento home lands in Phase 2 — up next, fresh episodes and premieres from your library."
    />
  );
}
