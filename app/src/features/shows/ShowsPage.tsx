import { PagePlaceholder } from "@/ui/PagePlaceholder";

export default function ShowsPage() {
  return (
    <PagePlaceholder
      title="My Shows"
      sub="Everything you follow, by status."
      note="Your library grid arrives in Phase 2 — watching, caught up, watchlist and finished buckets."
    />
  );
}
